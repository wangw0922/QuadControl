# iOS Broadcast Extension boundary

Future entry point: ReplayKit Broadcast Upload Extension target, built with its containing Xcode project via `xcodebuild build`. It requires user action in the system broadcast picker. No extension is implemented or validated; Screen Curtain behavior remains P0 blocked until device testing.
