# Product constraints

All control and capture require visible, user-initiated authorization. Hidden control, secret recording, lock-screen bypass, root, jailbreak, private APIs, and default unattended access are prohibited. Android M0 is read-only discovery; it does not pair or connect.
