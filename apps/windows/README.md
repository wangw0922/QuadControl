# Windows app boundary

Future entry point: WinUI 3 desktop host calling Rust FFI. Prerequisites: Windows 11, .NET SDK and Windows App SDK. Intended command: `dotnet build`. No project or UI exists yet; this host was not verified in M0.
